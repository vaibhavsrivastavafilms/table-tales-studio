import OsPlaceholder from "@/components/os/OsPlaceholder";

export default function SettingsPage() {
  return (
    <OsPlaceholder
      title="Settings"
      description="Outlets, users, roles, integrations, and workspace preferences."
    />
  );
}
