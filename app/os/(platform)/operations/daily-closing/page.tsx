import OsPlaceholder from "@/components/os/OsPlaceholder";

export default function DailyClosingPage() {
  return (
    <OsPlaceholder
      title="Daily Closing"
      description="End-of-day reconciliation for cash, cards, and outlet handover."
    />
  );
}
