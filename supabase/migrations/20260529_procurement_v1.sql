-- DEPRECATED · Superseded by 20260606_procurement_os_v2.sql
-- Do not create enums or tables here — v1 duplicates v2 and breaks enum creation on fresh installs.
-- Kept as a no-op so existing migration history timestamps remain stable.

select 1 as procurement_v1_deprecated;
